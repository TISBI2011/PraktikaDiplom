<Page x:Class="Praktika2011.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Praktika2011.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="LoginPage">

    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="200">

            <TextBlock Text="Login" />
            <TextBox x:Name="TBLogin" />
            <TextBlock Text="Password" />
            <TextBox x:Name="TBPassword" />
            <TextBlock Text="Secret word" />
            <TextBox x:Name="TBSecret" />
            <Button x:Name="BLogin"
                    Content="Login"
                    Click="BLogin_Click" />
            <DatePicker x:Name="DPDate" Style="{DynamicResource CustomDatePickerStyle}" />
        </StackPanel>
    </Grid>
</Page>
